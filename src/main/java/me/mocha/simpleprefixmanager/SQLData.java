package me.mocha.simpleprefixmanager;

import org.bukkit.entity.Player;

import java.util.UUID;

public class SQLData {

    public String loadFromSQL(Player player){
        UUID uuid = player.getUniqueId();

        return "";
    }
}
